Component({
  properties: {
    // 这里定义了imageUrl属性，属性值可以在组件使用时指定
    imageUrl: {
      type: String,
      value: 'default value',
    },
    textContent: {
      type: String,
      value: 'default value',
    }
  }
})